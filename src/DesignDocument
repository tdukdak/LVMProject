PHD(physical Hard Drive)constructs the bottom of the hierarchy
PV(physical volume) extends PHD and using a PHD private instance variable that will be designated to each PV
VG(volume groups) extends PV and creates a list of both LV's and PV's to track and report back those lists whenever also calculates space
LV(Logical volume) extends VG and holds a volume group.